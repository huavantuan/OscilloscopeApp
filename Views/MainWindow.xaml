<Window x:Class="OscilloscopeApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:vm="clr-namespace:OscilloscopeApp.ViewModels"
        xmlns:sp="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        Title="Oscilloscope" Width="1100" Height="700">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="10" Spacing="8">
            <ComboBox Width="120" ItemsSource="{Binding Serial.AvailablePorts}" SelectedItem="{Binding Serial.SelectedPort}" />
            <TextBox Width="100" Text="{Binding Serial.BaudRate}" />
            <Button Width="110" Content="{